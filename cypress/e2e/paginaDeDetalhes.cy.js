import { faker } from "@faker-js/faker";

// Ver como colocar o id do usuário no before each

describe("Testes de usuário na página de detalhes", () => {
  it("Deve conseguir alterar cadastro de usuário", () => {});

  it("Checando se as alterações foram efetuadas", () => {});

  it("Não deve ser possível editar um id com mais de 100 caracteres", () => {});

  it("Não deve ser possível editar um id com menos de 4 caracteres", () => {});

  it("Não deve ser possível editar um nome com mais de 100 caracteres", () => {});

  it("Não deve ser possível editar um nome com menos de 4 caracteres", () => {});

  it("Não deve ser possível editar um e-mail com mais de 60 caracteres", () => {});

  it("Não deve ser possível editar um e-mail com menos de ? caracteres", () => {});

  it("Deletar usuário", () => {});
});
